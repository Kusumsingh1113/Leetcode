bool isPalindrome(int x) 
{
    long long rev=0;
    int r, f=x;
    while(x>0)
    {
        r=x%10;
        rev=rev*10+r;
        x=x/10;
    }
    if(rev==f)
        return true;
    else
    return false;
}
